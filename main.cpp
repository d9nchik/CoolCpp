#include<string>
#include <iostream>

#define multiply(a, b) ((a)*(b)) //multiply macro from first exercise

using namespace std;

int main() {
    cout << multiply(5-1, 3-1) << endl;
    return 0;
}
